using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Painter : MonoBehaviour
{
    //public Color Color = Color.black;
    //public float SpreadRadius = 0.1f;
    //public float Range = 10f;



    //private void Update()
    //{
    //    if (Input.GetButton("F5"))
    //        return;

    //    this.transform.GetPositionAndRotation(out Vector3 position, out Quaternion rotation);

    //    bool foundSurface = Physics.SphereCast(
    //        position,
    //        this.SpreadRadius,
    //        rotation * Vector3.forward, out RaycastHit hit, this.Range);
    //    if (foundSurface && hit.collider.TryGetComponent(out PaintableSurface surface))
    //        surface.Paint(this, hit);
    //}
}
